name: anti-copyright-bot
description: Telegram bot to delete media and prevent copyright claims
version: 1.0.0

language: python

services:
  bot:
    build:
      run: pip install -r requirements.txt
    run: python3 main.py
    restart: always
